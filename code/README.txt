The code directory is largely dedicated to the analysis of Mercury's magnetosphere by looking at when and how frequent boundary crossings happen.

To start, go to the data_handling directory and collect the downsampled position data with downsample_spice_data.py.

Then, run peak_finder_orbits.py, so that the orbits can be defined.

Finally, you can run any of the histogram plotters in the plots directory.
